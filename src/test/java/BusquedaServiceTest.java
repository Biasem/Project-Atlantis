import com.example.atlantis.model.Regimen;
import com.example.atlantis.service.BusquedaService;
import org.junit.runner.RunWith;
import org.mockito.junit.MockitoJUnitRunner;
import org.junit.Before;
import org.junit.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import static org.mockito.ArgumentMatchers.any;
import static org.junit.jupiter.api.Assertions.*;

@RunWith(MockitoJUnitRunner.class)
public class BusquedaServiceTest {

    @InjectMocks
    BusquedaService busquedaService;

    @Before
    public void hotelesPrueba(){

    }
    @Test
    public void pruebaBusquedaTest(){
//        Mockito.when(regimenRepository(any())).thenReturn(regimen);
//        Regimen regimenGuardada = regimenService.guardarRegimen(regimen);
//        assertNotNull(regimenGuardada);
//        assertEquals(regimenGuardada,regimen);

    }

}
